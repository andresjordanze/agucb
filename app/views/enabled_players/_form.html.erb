<%= form_for(@enabled_player) do |f| %>
  <% if @enabled_player.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@enabled_player.errors.count, "error") %> prohibited this enabled_player from being saved:</h2>

      <ul>
      <% @enabled_player.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :match_id %><br>
    <%= f.number_field :match_id %>
  </div>
  <div class="field">
    <%= f.label :team_id %><br>
    <%= f.number_field :team_id %>
  </div>
  <div class="field">
    <%= f.label :player_id %><br>
    <%= f.number_field :player_id %>
  </div>
  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
